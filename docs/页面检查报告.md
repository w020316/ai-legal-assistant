# linzAI 法律助手 — 页面检查报告（v1.13.0）

> **检查日期**：2026-09-08
> **检查范围**：全部 11 个视图/弹窗组件的响应式、布局、视觉一致性与兼容性
> **目标分辨率**：1920×1080（桌面）、1366×768（笔记本）、768×1024（平板竖屏）、375×667（移动端）

---

## 一、响应式断点覆盖矩阵

| 视图 | 桌面默认 | ≤1024px(平板横屏) | ≤768px(平板竖屏/手机) | ≤480px(超小屏) |
|------|:---:|:---:|:---:|:---:|
| MainLayout（骨架/侧边栏） | ✅ | — | ✅ | ✅ |
| LoginView | ✅ | — | ✅ | ⚠️ |
| ChatView | ✅ | — | ✅ | ⚠️ |
| DocumentView | ✅ | — | ✅ | ⚠️ |
| TemplateView | ✅ | — | ✅ | ⚠️ |
| CaseView | ✅ | ✅ | ✅ | ✅ |
| HealthView | ✅ | ✅ | ✅ | ✅ |
| AuditView | ✅ | — | ✅ | ⚠️ |
| PrivacyView / TermsView | ✅ | — | ✅ | — |
| VersionUpdateDialog | ✅ | — | ✅ | — |

> ✅=有专门规则；⚠️=若仅依赖 768px 规则，375px 下布局可用但无超小屏压缩

## 二、按目标分辨率评估

| 分辨率 | 结论 |
|--------|------|
| 1920×1080 | ✅ 桌面默认 + 柔性栅格，无超宽溢出；顶栏/侧边栏固定宽，内容区自适应居中 |
| 1366×768 | ✅ 桌面布局正常，侧边栏+内容拖蓝，标题行未挤压 |
| 768×1024（平板竖屏） | ✅ 全部视图命中 768px 断点：侧边栏转抽屉、左右栏改上下、弹窗 92vw |
| 375×667（移动端） | ⚠️ 仅 MainLayout/CaseView/HealthView 有 ≤480px 优化；ChatView/DocumentView/TemplateView/LoginView/AuditView 依靠 768px 规则，可正常使用但字号/间距/顶部信息栏未做超小屏压缩 |

## 三、发现的问题与修复清单（分级）

| ID | 级别 | 页面 | 问题 | 建议修复 | 状态 |
|----|------|------|------|----------|------|
| PG-1 | P2 | ChatView | 375px 下输入区与消息操作栏（复制/重生成/反馈）可能横向拥挤 | ≤480px：操作栏图标缩小、时间戳隐藏；输入框命令区两行 | ✅ v1.13.1 已实现（480px 压缩） |
| PG-2 | P2 | DocumentView | 375px 下 doc-header 操作按钮（开始分析/导出/比对）堆叠拥挤 | ≤480px：按钮收缩、操作栏换行、info-bar 压缩 | ✅ v1.13.1 已实现 |
| PG-3 | P2 | TemplateView / LoginView / AuditView | 无 ≤480px 规则，抽屉/表单边距偏大 | 追加 480px 间距压缩（与 CaseView 同口径） | ✅ v1.13.1 已实现 |
| PG-4 | P3 | 全站 | 部分视图 header 未隐藏 masthead-eyebrow（副标题），超小屏信息冗余 | 480px 隐藏装饰性 eyebrow（MainLayout 已实现，其余视图对齐） | ✅ v1.13.1 已实现（全局 .page-title 480px 隐 hide + ChatView eyebrow） |
| PG-5 | P3 | 全站 | 颜色/字体走 CSS 变量（--color-* / --font-*），一致性好；仅少量内联十六进制（#FBF8F1 等纸白）分散 | 抽引至设计系统变量，统一维护 | 📋 登记 |

## 四、视觉一致性与兼容性

- **设计系统**：基于 `ui-skill` / `docs/UI设计规范文档.md` 的 CSS 变量（主色牛血红 `--color-accent #7A1F2B`、古铜金 `--color-gilt`、象牙纸 `--color-bg-card #FBF8F1`、衬线/等宽字体栈），跨页面一致。
- **字体**：Cormorant Garamond / Source Serif 4 / Noto Serif SC / JetBrains Mono，中文字体回退栈完整。
- **图片**：无外部远程图片依赖（favicon.svg + 纯 CSS/SVG 装饰），不存在图片加载失败风险。
- **交互反馈**：按钮 hover/active、折叠面板过渡、SSE 流式光标、进度脉冲均已实现动画反馈。
- **跨浏览器**：使用标准 CSS（flex/grid + 变量、`var(--ease-out)` 自定义缓动），Element Plus 组件 + 原生 Web API，兼容 Chrome/Firefox/Safari/Edge 最新两个版本（无需 -webkit- 前缀的现代用法为主）；`v-html` 渲染的 markdown 已 `html:false` 防 XSS。

## 五、结论

响应式整体覆盖优秀（1920/1366/768 全通过），**375px 超小屏为唯一系统性缺口**（PG-1~PG-4，建议作为 P2 一批实现）。视觉一致性、字体、交互反馈、跨浏览器兼容性良好，未发现布局破损或内容错误。

---
*报告生成时间：2026-09-08（v1.13.0）*

---

## 六、v1.16.1 再核验（2026-09-10）

> 质量评估阶段三：以浏览器子代理对**线上 pages.dev** 真实渲染实测，并用源码响应式规则核验未覆盖分辨率。全程如实标注，不编造测量数据。

### 6.1 真实浏览器实测（工具可用视口 700×610，介于 768/992 断点档）

登录 admin/admin123 成功进入主界面后，实测 4 个核心页面于 700px 档：

| 页面 | 横向滚动 | 元素越界 | 空态 | 侧栏/菜单 | 字号(px) |
|------|:---:|:---:|:---:|:---:|:---:|
| 智能问答 | ✅ 无 (scrollWidth=viewport) | ✅ 无 | ✅ 推荐卡片空态正常 | ✅ el-menu 正常 | 12–28 无重叠 |
| 案例检索 | ✅ 无 | ✅ 无 | — | ✅ | ✅ |
| 文书范本 | ✅ 无 | ✅ 无 | — | ✅ | ✅ |
| 文档分析 | ✅ 无 | ✅ 无 | ✅ "暂无文档"正常 | ✅ | ✅ |

### 6.2 源码级响应式核验（补齐 375/768/1024/1366/1920）

- ✅ `index.html` 含 `<meta name="viewport" content="width=device-width, initial-scale=1.0">`，移动端缩放无错乱。
- ✅ 六档断点齐全并覆盖全部 14 个视图/组件：`≤480px`(超小屏) / `≤767px`(移动抽屉) / `768` / `992` / `1024`(平板横屏) / `1200` / `1920`。
- ✅ `MainLayout.vue:38` 用 `window.matchMedia('(max-width:768px)')` 动态切换侧边栏为抽屉，移动端含汉堡菜单。
- ✅ 正文容器均以 `max-width`(720/860/1080px) 自适应居中，无固定超宽造成横向溢出。
- ⚠️ P3 观察：多处 `white-space:nowrap`（CaseView/TemplateView/DocumentView/MessageItem/SessionList），极窄屏下长标题/长用户名可能轻微溢出——未在本次实测复现，登记跟踪。

### 6.3 未覆盖项（如实标注）

浏览器自动化工具**无 device-emulation 能力**（视口固定），因此 `1920×1080 / 1366×768 / 375×667` 未能在真实浏览器渲染验证，由 6.2 源码规则路径补足。上表 700px 档 + 断点核验共同给出置信度，UI 无系统性布局破损。

### 6.4 结论

沿用 v1.13 断点矩阵与修复成果（PG-1~PG-4 已实现），本轮实测确认核心页面在需断点档无横向滚动/越界/空态异常，未发现需返工的布局缺陷。

---
*v1.16.1 页面再核验：2026-09-10*