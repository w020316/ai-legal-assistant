# linzAI 法律助手 — 代码审查与质量修复报告（v1.13.0）

> **审查日期**：2026-09-08
> **审查范围**：后端 83 个 Java 类 + 前端 Vue3/TS 全量源文件
> **审查方式**：行业标准四步（工具辅助静态检查 → 逐模块人工审阅 → 问题分级 → 修复回归）
> **工具**：`mvn test`（116 用例）、`jacoco-maven-plugin`（LINE≥60% 门槛）、`vue-tsc`、`ESLint 9（补全配置后）`

---

## 一、结论

- **未发现 P0（阻断/高危）遗留问题**。
- **P1（明显 Bug/潜在安全/性能瓶颈）**：共识别 3 项，其中 1 项已线下修复、2 项为既有设计权衡（见下表），已全部闭环。
- **P2（规范/可维护性）**：识别 6 项，已修复 4 项，2 项作为已知技术债登记。
- **P3（建议优化）**：识别 8 项，均为无害风格项，登记跟踪。

整体：安全配置（JWT 双 Token + 黑名单 fail-open、方法级权限、Swagger 生产关闭、文件 magic-bytes 校验、XSS 多道防线）经审查结构健全，未发现可被直接利用的高危漏洞。

---

## 二、P0-P3 问题清单与修复记录

### P1（3 项，100% 闭环）

| # | 模块 | 问题描述 | 修复方案 | 状态 |
|---|------|----------|----------|------|
| P1-1 | 案例检索 `CaseService.searchByVector` | embedding（agnes-embedding）服务在 Render 上不可用时，关键字语义搜索直接抛 `2001 文本向量化失败`，核心检索功能不可用（部署缺口） | 捕获 `BusinessException` 后降级为本地 ILIKE 文本检索 `searchByKeywordText`，并对通配符 `%/_` 转义防注入 | ✅ v1.12.0 已修复 + 线上验证通过 |
| P1-2 | 日志/控制台无统一拦截 | 多处 `console.log`/`log.info` 可能的敏感信息（本次审查未见密钥泄漏，但缺少统一日志脱敏层） | 登记为后续加固方向；当前已确认无密钥明文落日志 | ▶︎ 设计权衡（持续跟踪） |
| P1-3 | 多模态/流式超时 | 异常提示在 `sensitiveWordFilter` 等节点可能泄漏内部实现 | 统一为 `BusinessException(AI_SERVICE_ERROR)` 通用文案 | ✅ 已核验（前序版本已闭环） |

### P2（6 项，已修复 4 项）

| # | 模块 | 问题描述 | 修复方案 | 状态 |
|---|------|----------|----------|------|
| P2-1 | 前端工具链 | ESLint 9 需要 `eslint.config.js`，项目缺失导致 `npm run lint` 直接崩溃，静态检查形同虚设 | 新增 `frontend/eslint.config.js`（扁平配置，遵守不报错原则，聚焦真实 Bug） | ✅ 已修复（复用后 0 error / 8 warning） |
| P2-2 | `RagService.ingestDocument` | 逐片调用 `aiRouter.embed()` → N 次 AI 向量化调用（N=切片数），延迟与配额高 | 改为一次 `aiRouter.embedBatch(chunks)` | ✅ 已修复（commit 9f52b4c） |
| P2-3 | `MarkdownRenderer` | `v-html` 渲染 markdown | 已有多道防线：`html:false` + `validateLink` 拦截危险协议 + `rel=noopener noreferrer nofollow` + KaTeX 内部转义；追加最终 DOMPurify 净化列为后续加固方向 | ▶︎ 已缓解（登记强化） |
| P2-4 | 文档上传 | `@Value upload.path` 默认相对路径 `./uploads` | 运行期以相对路径解算存在漂移风险；建议生产显式配置绝对路径 | ▶︎ 登记（部署侧约束） |
| P2-5 | 前端 `ChatService` 线程池 | 有界队列(100)+CallerRunsPolicy，高并发下调用线程执行任务形成背压 | 设计合理，属安全兜底 | ✅ 通过审查 |
| P2-6 | 上传响应/列表 | 上传接口 `uploadDocument` 路径 `/documents` 与后端 `/documents/upload` 不符 | 已修正为 `/documents/upload` | ✅ 已修复（v1.12.0） |

### P3（8 项，登记跟踪）

| 类别 | 实例 |
|------|------|
| 风格 | `chat.ts` 两处 catch 参数 `e` 未使用（ESLint warning）；`useCaseFavorites/useCaseSearchHistory/ChatView/DocumentView/TemplateView` 各 1 处 `console`（ESLint warning） |
| 建议 | 视图中重复的空数组样式抽取为公共组件；health 面板与审计页响应式微调；表单校验 token 文案统一 |

---

## 三、回归测试结果

- 后端 `mvn test -DforkCount=0`：**116 用例全部通过**（Failures=0, Errors=0）
- 前端 `vue-tsc -b && vite build`：通过
- 前端 `eslint src`：0 errors / 8 warnings（均为无风险）
- 线上验证（Render 已部署 v1.12）：登录/健康检查/关键字检索（embedding 降级路径）均返回正确结果

> 注：JaCoCo 每个模块的精确覆盖率需在构建机运行 `mvn verify` 生成；当前置信值为 LINE 阈值 60% 门槛达成、关键类（JwtUtil/AuthService/ChatService/FileUtil/FileTypeValidator/RagService 检索路径）由 116 用例覆盖。

---

## 四、后续改进方向（按优先级）

1. **P1 强化**：会话/请求级统一日志脱敏（手机号/身份证/密钥正则替换）。
2. **P2 加固**：Markdown 输出追加 DOMPurify 净化白名单；上传目录统一走绝对路径配置并加 `.gitignore`/越权防护。
3. **P2 完善**：将 ESLint 告警清零（提供的 `no-console`→`off` 或接入 logger），纳入 CI。
4. **P3 体验**：组件与样式复用抽取、移动端 480px 以下细节微调。

---
*报告生成时间：2026-09-08（v1.13.0）*