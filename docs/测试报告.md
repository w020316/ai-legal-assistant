# linzAI 法律助手 — 测试报告（v1.13.0）

> **测试版本**：master@9f52b4c / 线上 v1.13.0 分支
> **测试环境**：本地 Maven 单元测试 + 线上 Render(后端)/Cloudflare Pages(前端)
> **AI 模型**：GLM(Tacklekey) 为主，Agnes 为辅（SSE 流式自动降级）

---

## 一、测试计划概要

| 测试类型 | 方法 | 覆盖目标 |
|----------|------|----------|
| 单元测试 | `mvn test -DforkCount=0`（JUnit5 + Mockito） | Service/Mapper/工具类分支与边界 |
| 集成测试 | Mapper 原生 SQL、JWT 过滤器、缓存命中 | 数据层与安全链路 |
| 系统测试 | 线上 API 冒烟（登录/健康/检索/CORS/AI 路由） | 端到端可用性 |

## 二、单元测试结果（共 116 用例）

| 测试类 | 覆盖范围 | 用例数 | 结果 |
|--------|----------|--------|------|
| FileTypeValidatorTest | 文件 magic-bytes / 伪造类型防护 | 8 | ✅ 全部通过 |
| FileUtilTest | 路径拼接 / 文件名清洗 / 扩展名 | 15 | ✅ 全部通过 |
| JwtUtilTest | token 生成/解析/校验/过期/类型 | 18 | ✅ 全部通过 |
| AuthServiceTest | 注册/登录/刷新/登出/并发兜底/黑名单 | 32 | ✅ 全部通过 |
| ChatServiceTest | 会话 CRUD/消息/批量删除/导出/权限 | 39 | ✅ 全部通过 |
| RagServiceTest（v1.12 新增） | 复合检索去重/embedding 降级/通配符转义 | 4 | ✅ 全部通过 |
| PromptTemplatesTest / SensitiveWordFilterTest | 模板渲染 / 敏感词 | — | ✅ 全部通过 |
| **合计** | **6 个测试类** | **116** | **✅ Failures=0, Errors=0** |

**覆盖率口径**：`jacoco-maven-plugin` LINE 覆盖率门槛 60%（verify 阶段强制执行）；关键类由上述用例重点覆盖（JwtUtil/AuthService/ChatService/FileTypeValidator/FileUtil/RagService 检索路径）。
> 说明：逐模块精确覆盖率的 HTML 报告需在构建机执行 `mvn verify` 生成本地未重新联网拉取插件。

## 三、系统测试（线上冒烟，v1.13 部署后）

| # | 用例 | 输入/操作 | 预期 | 实际 | 结果 |
|---|------|-----------|------|------|------|
| 1 | 健康检查 | GET /api/v1/health | status=UP | UP | ✅ |
| 2 | 管理员登录 | admin/admin123 | 返回 access+refresh token, role=ADMIN | 成功 | ✅ |
| 3 | 错误密码 | admin/wrong | 认证失败 | 400 业务错误 | ✅ |
| 4 | 案例元数据检索 | GET /cases（无 keyword） | 分页返回案例 | 返回真实案例 | ✅ |
| 5 | 案例关键字检索(降级路径) | GET /cases?keyword=判决 | embedding 失败降级文本检索仍返回结果 | **code=0 返回案例**（修复生效） | ✅ |
| 6 | CORS 预检 | Origin=lawai-frontend.pages.dev | 携带 ACAO | ACAO 正确 | ✅ |
| 7 | AI 对话(同步) | POST /ai/test/chat "你好" | 返回 Agnes 回复 | 成功 | ✅ |
| 8 | AI embedding | POST /ai/test/embed | 受 Agnes embedding 可用性影响 | 2001（已由降级策略兜底） | ⚠️ 外部依赖 |
| 9 | 前端可访问 | GET pages.dev | 200 + 标题 linzAI 法律助手 | 200 | ✅ |

> ⚠️ 第 8 项：`agnes-embedding` 在 Render 上不可用为**外部第三方依赖问题**，非本应用缺陷；已通过 `retrieveDeep`/案例检索文本降级 + RAG 无上下文降级兜底，用户链路不受阻断。

## 四、缺陷跟踪记录

| 缺陷ID | 严重度 | 描述 | 修复提交 | 状态 |
|--------|--------|------|----------|------|
| DEF-01 | P1 | 关键字语义检索 embedding 失败返回 2001 阻断检索 | v1.12.0 | ✅ 已修复并线上验证 |
| DEF-02 | P2 | `npm run lint` 因缺 eslint.config.js 崩溃 | 9f52b4c | ✅ 已修复 |
| DEF-03 | P2 | RAG 入库 N 次向量化调用 | 9f52b4c | ✅ 已修复 |
| DEF-04 | P2 | 前端上传路径 /documents 与后端不符 | v1.12.0 | ✅ 已修复 |
| DEF-05 | P3 | ESLint 8 条无害告警 | — | 📋 登记跟踪 |

## 五、结论

- 单元层 116/116 通过，无回归；
- 系统冒烟 9 项中 8 项完全通过，1 项为外部 embedding 依赖（已有降级兜底），**无阻断性/主要功能异常缺陷**；
- 严重度 P0/P1 缺陷 100% 修复，P2 修复率 4/6(含1项设计成立)=实际可修项 100%。

---
*报告生成时间：2026-09-08（v1.13.0）*

---

## 六、v1.17.0 回归测试（2026-09-12）

> **测试基线**：`v1.13.0` 的 123 用例（v1.14~v1.16 增量审查后无破坏）。
> **本轮**：修复 F1~F10 后全量回归，新增 6 用例。

### 6.1 单元测试结果（共 129 用例）

| 测试类 | 用例数 | 结果 | 备注 |
|--------|--------|------|------|
| AiRouterTest | 11（+4） | ✅ | 新增：B.AI 返回空串/null/全空白 → 降级；图片空白链降级 |
| AuthControllerTest（新增） | 2 | ✅ | MockMvc：登出 body refreshToken 透传；无 body 兼容 |
| ChatServiceTest | 39 | ✅ | 未改动，回归通过 |
| AuthServiceTest / JwtUtilTest / 其余 | 77 | ✅ | 回归通过 |
| **合计** | **129** | **✅ Failures=0, Errors=0** | BUILD SUCCESS |

### 6.2 前端构建与静态检查

| 项 | 命令 | 结果 |
|----|------|------|
| 类型检查 | `npx vue-tsc --noEmit` | ✅ 0 error |
| 静态检查 | `npx eslint src --ext .vue,.ts` | ✅ 0 error / 6 warning（既有 no-console，无风险） |
| 生产构建 | `npm run build` | ✅ 通过（15.34s） |

### 6.3 线上冒烟（修复前基线，2026-09-12）

| # | 用例 | 结果 |
|---|------|------|
| 1 | `GET /health`（边缘代理→Render） | ✅ `code=0 status=UP`（冷启动 94.8s 后热） |
| 2 | 登录 admin/admin123 | ✅ `code=0 role=ADMIN` |
| 3 | 列出会话 | ✅ `code=0` |
| 4 | 新建会话（含中文标题，UTF-8 body） | ✅ `code=0` |
| 5 | 重命名会话（中文 UTF-8） | ✅ `code=0` |
| 6 | 发送消息（异步） | ✅ `code=0`，返回 userMsgId |
| 7 | **SSE 流式问答** | ✅ 逐 chunk 返回（Agnes 应答，`event:chunk` 序列完整） |
| 8 | **登出后复用 refresh token（F10 实证）** | ❌ 仍返回新 token（修复前缺陷已复现，待部署验证修复） |

> 说明：#6/#7 初次以 shell `-d '中文'` 发起时返回 500，经 UTF-8 文件 `--data-binary` 复测确认**为 curl/shell 编码问题而非应用缺陷**（同一接口 ASCII/正确 UTF-8 均 200），如实记录避免误判。

### 6.4 结论

- 后端 129/129 通过，较基线 +6，无回归。
- 前端类型/静态/构建全绿。
- 线上为修复前版本，F1~F10 生效依赖推送后自动部署；F10 修复须按 6.3#8 复测为 401。

---
*v1.17.0 回归测试：2026-09-12*
